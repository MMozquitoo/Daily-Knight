/**
 * Clothing Detail screen
 *
 * Shows:
 *   - Template preview (shape + color)
 *   - Editable fields: type, color, formality, weather, context, availability
 *
 * Primary action: Save
 * Secondary: Mark unavailable / Delete
 *
 * Route param: [id] — wardrobe item ID
 */

export default function ClothingDetailScreen() {
  return null;
}
