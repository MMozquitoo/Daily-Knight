/**
 * Onboarding step 2 — Permissions
 *
 * Permission cards:
 *   - Weather (location access)
 *   - Calendar (agenda context)
 *   - Camera (add clothes faster)
 *
 * Primary action: "Allow access"
 * Secondary: "Set up later"
 *
 * Uses expo-location, expo-calendar, expo-image-picker permission APIs.
 */

export default function PermissionsScreen() {
  return null;
}
