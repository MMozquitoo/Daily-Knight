/**
 * Onboarding step 3 — Style Preference
 *
 * Three choices: Casual | Formal | Mixed
 * Saves to user profile in local DB.
 * One primary action: "Save preference"
 */

export default function StyleScreen() {
  return null;
}
