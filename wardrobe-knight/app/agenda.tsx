/**
 * Agenda screen
 *
 * Accessed from Home (not a tab).
 *
 * Shows:
 *   - Today's events list with formality tags (Formal, Work, Casual, Travel)
 *   - Day summary (e.g. "Office day with 1 formal meeting")
 *   - Manual override: mark today as casual / formal
 *
 * Primary action: "Adjust day context" → re-runs engine with updated context.
 */

export default function AgendaScreen() {
  // TODO: Read calendar events from useDayStore, display with overrides
  return null;
}
