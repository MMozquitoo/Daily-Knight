/**
 * Database Client
 *
 * Initializes expo-sqlite connection and Drizzle ORM instance.
 * Runs migrations on first load.
 *
 * Exports:
 *   - db: Drizzle database instance
 *   - initDatabase(): Promise<void> — called once in root layout
 */

// TODO: Initialize expo-sqlite + Drizzle, run migrations
