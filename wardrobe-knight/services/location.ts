/**
 * Location Service
 *
 * Gets user's current location via expo-location.
 *
 * Uses low accuracy (city-level) — street precision is unnecessary.
 * Reverse geocodes to get display name for Home screen TopBar.
 *
 * Exports:
 *   - getCurrentLocation() → { lat, lon, displayName }
 */

// TODO: Implement location fetching + reverse geocode
